nt build.xml in this directory allows one to build the different
packages for the Google Secure Data Connector. Both the DEB/RPM builds 
use the --lsb as the default prefix.

To build all packages or for a product release run: ant all

To build a specific package use the following:
DEB: ant deb
RPM: ant rpm
